struct Stack
{
  unsigned int StackVal[128];
  struct Stack* last;
  unsigned int* top;
}
bool addDataToStack(struct Stack** stack,unsigned int data)
{
  if ((**stack).top==126){
    
  }else{
    
  }
}